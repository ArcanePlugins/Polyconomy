package io.github.arcaneplugins.polyconomy.plugin.bukkit.economy.account

// made to resemble the causes available in Treasury,
// package:me.lokka30.treasury.api.common interface:Cause
enum class PolyCause {
    PLAYER,
    NON_PLAYER,
    PLUGIN,
    SERVER,
}